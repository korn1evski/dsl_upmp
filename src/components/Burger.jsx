import React, {useState} from 'react';
import {RxHamburgerMenu} from 'react-icons/rx'

function Burger({isShown, setIsShown}) {

    return (
        <div className={'absolute top-[40px] left-[40px]'}>
        </div>
    );
}

export default Burger;